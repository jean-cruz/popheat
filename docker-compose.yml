services:
  iris:
    image: intersystems/iris-community:2026.2
    container_name: popheat-iris
    ports:
      - "1972:1972"
      - "52773:52773"
    volumes:
      - ./src:/opt/popheat/src
      - ./data:/opt/popheat/data
      - iris-data:/durable

volumes:
  iris-data:
