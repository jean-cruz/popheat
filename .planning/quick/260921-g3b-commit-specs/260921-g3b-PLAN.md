---
phase: quick-260921-g3b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/dashboard-api.spec
  - specs/heat-classification.spec
  - specs/ingestion-pipeline.spec
  - specs/popularity-model.spec
  - specs/telemetry.spec
  - specs/venue-sourcing.spec
autonomous: true
quick_id: 260921-g3b
must_haves:
  truths:
    - "The specs directory is tracked by git: `git status --porcelain -- specs/` prints nothing after the commit"
    - "HEAD is a single commit whose file list is exactly the spec files that were untracked under specs/ at execution time (six at planning time) and nothing else"
    - "The commit subject follows Conventional Commits (`docs(specs): ...`) and the message ends with the Co-Authored-By: Claude Opus 5 trailer"
    - "Unrelated working-tree changes (.planning/config.json, .planning/milestone.lock, .planning/state.json, .claude/worktrees/, .gsd/) are still uncommitted and still show in git status exactly as before"
  artifacts:
    - "A new commit on main touching only paths under specs/"
  key_links:
    - "Pre-stage guard: the index must be empty before git add, so no unrelated staged content can ride along into the specs commit"
    - "Post-stage guard: every staged path is under specs/ before git commit runs"
---

<objective>
Bring the six untracked behavioral spec files under `specs/` into version control with one Conventional-Commits-style commit on `main`, without touching any other pending working-tree change.

Purpose: `specs/` is the project's source of truth for behavior (CLAUDE.md's compliance constraint cites `specs/popularity-model.spec`), yet it has never been committed (zero commits in history touch it). Untracked specs cannot be cited by commit SHA, reviewed in a PR, or shipped in the Open Exchange package. This is a pure git operation: no file content changes.

Output: One commit on `main` containing exactly the files that git reports as untracked under `specs/` at execution time (six at planning time: dashboard-api.spec, heat-classification.spec, ingestion-pipeline.spec, popularity-model.spec, telemetry.spec, venue-sourcing.spec), with subject `docs(specs): ...` and the required trailer.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

Working tree observed at planning time (2026-09-21, branch `main`, git 2.34.1, no hooks, gpgsign unset):

```
 M .planning/config.json
?? .claude/worktrees/
?? .gsd/
?? .planning/milestone.lock
?? .planning/state.json
?? specs/
```

`specs/` contains exactly six files, none ignored (`git check-ignore` matches nothing), and `git log -- specs/` is empty. Only `specs/` is in scope. Everything else listed above is out of scope for this task and must remain uncommitted; the orchestrator commits the `.planning/` artifacts (PLAN, SUMMARY, STATE) itself afterward.

MUTABLE-SCOPE AUTHORITY: the snapshot above is informational. The executor re-observes `git status --porcelain -- specs/` and `git ls-files --others --exclude-standard -- specs/` live and commits what git reports at that moment, not what this plan lists.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage and commit the untracked specs/ directory as one docs(specs) commit</name>
  <files>specs/dashboard-api.spec, specs/heat-classification.spec, specs/ingestion-pipeline.spec, specs/popularity-model.spec, specs/telemetry.spec, specs/venue-sourcing.spec</files>
  <precondition>`git branch --show-current` prints `main`, and `git ls-files --others --exclude-standard -- specs/` prints at least one path (specs/ is still untracked; if it prints nothing, the work is already done: halt and report a no-op instead of creating an empty commit).</precondition>
  <action>
    Run every command from the repository root `/home/jean/git/popheat`. Do not edit, create, move, or reformat any file; this task only changes git state.

    Step 1, observe live scope (do not trust the planning-time snapshot). Capture the live untracked set with `EXPECTED=$(git ls-files --others --exclude-standard -- specs/ | sort)` and print it. Also run `git status --porcelain -- specs/`. The expected result is six `.spec` files and nothing tracked/modified under specs/. If the count differs from six, proceed with the live set anyway (that set is the authority) but record the discrepancy in the SUMMARY. If any path under specs/ shows as tracked-and-modified (status ` M`/`MM`) rather than untracked, stop and report: that contradicts the "never committed" premise and needs a human decision, not an automatic commit.

    Step 2, confirm nothing under specs/ is gitignored: `git check-ignore -q -- specs/* ; echo $?` must print `1` (no match). If it prints `0`, stop and report which path is ignored; do not force-add with `-f`.

    Step 3, index guard. Run `git diff --cached --quiet` and require exit 0 (empty index). If the index already holds staged content, stop and report what is staged (`git diff --cached --name-only`); do not unstage anything on the user's behalf and do not proceed, because a commit would fold that content in.

    Step 4, stage only specs: `git add -- specs/`. Then confirm the staged set is confined to that directory with the pathspec exclusion `git diff --cached --name-only -- . ':(exclude)specs'` which must print nothing, and confirm `git diff --cached --name-only | sort` equals `$EXPECTED`. Never run `git add -A`, `git add .`, or `git commit -a`; the out-of-scope items in the working tree (the modified config.json, the untracked milestone.lock / state.json, the worktrees and .gsd directories) must not be staged.

    Step 5, commit with chained `-m` flags so the trailer is a paragraph of its own (no heredoc needed):
    subject `docs(specs): add behavioral specs for pipeline, model, classification, API, telemetry`;
    body paragraph: "Track the six .spec files under specs/ that define PopHeat's behavior: venue sourcing (OSM catalog), popularity model (synthetic only, no scraped busy-now source), heat classification, ingestion pipeline (IRIS PyProd), telemetry, and dashboard API. CLAUDE.md already references specs/popularity-model.spec as the compliance source of truth, but the directory had never been committed.";
    final paragraph exactly `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.
    Adjust the word "six" in the body if the live set size differed in Step 1.

    Step 6, self-check before declaring done: `git status --porcelain -- specs/` prints nothing; `git show --name-only --format= HEAD | sort` equals `$EXPECTED`; `git log -1 --format=%B` ends with the trailer line; and the out-of-scope entries from Step 1's full `git status --porcelain` are all still present and unchanged. Do NOT commit any `.planning/` artifact (this PLAN, the SUMMARY, STATE.md); leave those for the orchestrator's docs commit.
  </action>
  <verify>
    <automated>cd /home/jean/git/popheat && SP=$(git status --porcelain -- specs/) && [ -z "$SP" ] && FILES=$(git show --name-only --format= HEAD) && [ -n "$FILES" ] && FILES=$(printf '%s\n' "$FILES" | sort) && [ "$(printf '%s\n' "$FILES" | tr '\n' ' ')" = "specs/dashboard-api.spec specs/heat-classification.spec specs/ingestion-pipeline.spec specs/popularity-model.spec specs/telemetry.spec specs/venue-sourcing.spec " ] && [ "$(printf '%s\n' "$FILES" | grep -vc '^specs/')" = "0" ] && SUBJ=$(git log -1 --format=%s) && printf '%s\n' "$SUBJ" | grep -Eq '^docs\(specs\): ' && MSG=$(git log -1 --format=%B) && [ "$(printf '%s\n' "$MSG" | grep -c '^Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>$')" = "1" ] && ST=$(git status --porcelain) && printf '%s\n' "$ST" | grep -q '^ M .planning/config.json$' && printf '%s\n' "$ST" | grep -q '^?? .planning/milestone.lock$' && printf '%s\n' "$ST" | grep -q '^?? .planning/state.json$' && echo VERIFY-OK</automated>
  </verify>
  <done>
    `git status --porcelain -- specs/` is empty; `git show --stat HEAD` lists exactly the six spec files (dashboard-api, heat-classification, ingestion-pipeline, popularity-model, telemetry, venue-sourcing) and no other path; the subject starts with `docs(specs): `; the message's last line is the `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` trailer; and the modified `.planning/config.json` plus the untracked `.planning/milestone.lock`, `.planning/state.json`, `.claude/worktrees/`, and `.gsd/` are still uncommitted exactly as before. If the live untracked set under specs/ differed from six files, the verify's explicit six-name check is replaced by equality with the live set captured in Step 1, and the SUMMARY records the difference.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree to git history | Untracked local files become permanent, shareable repository content |

## STRIDE Threat Register

Scope is a git commit of six plain-text documentation files already present on disk. No code executes, no dependency is installed, no network call is made, no secret is generated. ASVS level 1, block-on high.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-g3b-01 | Information Disclosure | git index / commit contents | low | mitigate | Index-empty guard (Step 3) and the `:(exclude)specs` staged-path check (Step 4) ensure only specs/ paths enter the commit, so the modified `.planning/config.json` and untracked `.planning/*.json|lock`, `.claude/worktrees/`, `.gsd/` cannot leak into history. Verify gate re-asserts those entries are still uncommitted. |
| T-g3b-02 | Tampering | commit authorship / message | low | mitigate | Message is fixed by the plan (Conventional Commits subject + required trailer) and grep-verified after commit; no `--amend`, `--no-verify`, or history rewrite is used. |
| T-g3b-03 | Repudiation | commit trail | low | accept | Standard git author metadata is sufficient for a solo documentation commit. |
| T-g3b-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable: this task installs nothing; no package-manager command is executed. |
</threat_model>

<verification>
Run the task's automated verify from the repo root after execution; it must print `VERIFY-OK`. Additionally `git log --oneline -1` should show the new `docs(specs): ...` commit on top of `699ad83`, and `git log --oneline -- specs/ | wc -l` should now print `1`.
</verification>

<success_criteria>
- One new commit exists on `main` whose file list is exactly the spec files that were untracked under `specs/` at execution time (six at planning time) and nothing else.
- Commit subject begins `docs(specs): ` and the message ends with the `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` trailer.
- `git status --porcelain -- specs/` is empty; the unrelated pending changes in `.planning/`, `.claude/worktrees/`, and `.gsd/` are untouched and uncommitted.
- No file content was created, edited, or deleted anywhere in the repository by this task.
</success_criteria>

<output>
Create `.planning/quick/260921-g3b-commit-specs/260921-g3b-SUMMARY.md` when done, recording the new commit SHA, the live file set that was committed, and any discrepancy from the six files expected at planning time. Do not commit the SUMMARY or this PLAN; the orchestrator handles the docs commit.
</output>
