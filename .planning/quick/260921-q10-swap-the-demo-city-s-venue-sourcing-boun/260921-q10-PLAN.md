---
phase: quick-260921-q10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/catalog_build.json
  - data/venues.json
  - data/venues.raw.json
  - iris/PopHeat/www/dashboard.html
autonomous: true
quick_id: 260921-q10
must_haves:
  truths:
    - "config/catalog_build.json's bounding_box is exactly {south: -23.575, west: -46.675, north: -23.545, east: -46.635} and is committed on main"
    - "GET http://localhost:52773/csp/popheat/api/venues returns at least one venue, and every returned venue's lat/lon falls inside that new bbox -- none fall inside the previous demo city's bbox (lat 41.139-41.151, lon -8.620--8.602)"
    - "The dashboard's initial Leaflet view (iris/PopHeat/www/dashboard.html) is built from the new bbox via fitBounds, not the old hardcoded center/zoom"
    - "No reading persisted before this swap remains visible as any venue's 'latest' reading -- PopHeat.Reading was purged and repopulated purely from the new catalog"
    - "The running popheat-iris container was never stopped via docker compose down -- only exec/read operations were used against it, so PopHeat.BatchTelemetry history and the production's auto-start registration survive untouched"
  artifacts:
    - "config/catalog_build.json (committed)"
    - "data/venues.json + data/venues.raw.json (regenerated locally, gitignored, not committed)"
    - "iris/PopHeat/www/dashboard.html (committed)"
  key_links:
    - "CatalogPollingService.on_poll() (popheat_pipeline/components.py) reads data/venues.json fresh on every 3-second tick with no caching -- overwriting the file is itself sufficient for the running production to start ingesting the new catalog, no restart required"
    - "PopHeat.Reading:LatestPerVenue (iris/PopHeat/Reading.cls) has no time-window cutoff, so a pre-swap reading for a venue_id no longer in the catalog would otherwise show forever as that venue's frozen 'current' state"
---

<objective>
Make Sao Paulo (Paulista / Jardins / Pinheiros / Vila Madalena, bbox south=-23.575 west=-46.675 north=-23.545 east=-46.635) the venue-sourcing bounding box for PopHeat's demo, replacing the Phase 1 bbox, and get the already-running IRIS pipeline and dashboard to reflect only that new catalog.

Purpose: the user has explicitly confirmed Sao Paulo as the permanent demo city going forward (not a temporary preview). specs/venue-sourcing.spec R3 already anticipates a bbox swap as a config-only, single-bounding-box change -- but "config-only" covers the catalog itself, not the two places that silently assumed the old bbox: the dashboard's hardcoded initial map view, and PopHeat.Reading's no-expiry "latest per venue" query, which would otherwise keep showing pre-swap venues forever alongside the new ones.

Output: config/catalog_build.json holds the Sao Paulo bbox; data/venues.json/venues.raw.json are regenerated from a live Overpass query against it; the dashboard's initial map view matches the new bbox; PopHeat.Reading no longer carries any pre-swap venue as a "latest" reading; the config + dashboard changes are committed on main.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@config/catalog_build.json
@scripts/build_catalog.py
@specs/venue-sourcing.spec
@popheat_pipeline/components.py
@iris/PopHeat/Reading.cls
@docker-compose.yml
@docker/init-production.sh

Working tree observed at planning time (2026-09-21, branch main): only `.planning/config.json` is modified (pre-existing, unrelated, out of scope for this plan -- do not stage it). `data/venues.json` and `data/venues.raw.json` are gitignored and untracked; `config/catalog_build.json` and `iris/PopHeat/www/dashboard.html` are clean/tracked.

Live baseline confirmed at planning time via `curl http://localhost:52773/csp/popheat/api/...`: the popheat-iris container is up, PopHeat.Production is running, the current catalog has 906 venues inside the old Porto-area bbox (lat 41.139-41.151, lon -8.620--8.602), and /counts currently reads `{"BAIXO":679,"MEDIO":103,"ALTO":124,"CRITICO":0}` summing to 906. These are the "before" numbers this plan's changes replace, not values to preserve.

MUTABLE-SCOPE AUTHORITY: re-observe live container/production state at execution time rather than trusting this snapshot -- if the container is down or the production isn't running, Task 3's precondition will catch it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Point the catalog builder at Sao Paulo and regenerate the venue catalog</name>
  <files>config/catalog_build.json, data/venues.json, data/venues.raw.json</files>
  <action>
    From repo root, edit config/catalog_build.json's "bounding_box" object only: set south=-23.575, west=-46.675, north=-23.545, east=-46.635 (Paulista/Jardins/Pinheiros/Vila Madalena, Sao Paulo). Leave amenity_allowlist, overpass_endpoint, request_timeout_seconds, overpass_timeout_seconds, and max_response_bytes untouched -- same JSON key order and 2-space indent as the rest of the file.

    Then regenerate the catalog against the new bbox: run `python3 scripts/build_catalog.py --config config/catalog_build.json` from repo root. This is a stdlib-only script (no venv/pip install needed) that POSTs a live Overpass QL query to the public endpoint and atomically overwrites data/venues.json + data/venues.raw.json on success; a failed fetch leaves any prior catalog file untouched and the script exits non-zero (VENU-06). It must print a summary line ("Fetched: N, dropped (missing field): D1, dropped (duplicate): D2, final catalog: F") and exit 0 -- if it does not, stop and report the error rather than letting Task 2/3 proceed against a stale or empty catalog. This exact host environment reached the public Overpass endpoint directly and successfully when the catalog was first built; if this run instead fails with a network/timeout error, retry once from inside the running container instead: `docker compose exec -T iris python3 /irisdev/app/scripts/build_catalog.py --config /irisdev/app/config/catalog_build.json` (repo root is bind-mounted at /irisdev/app).
  </action>
  <verify>
    <automated>cd /home/jean/git/popheat && python3 -m unittest tests.test_build_catalog 2>&1 | tail -3 | grep -Eq '^OK' && python3 -c "
import json
with open('config/catalog_build.json') as f:
    bbox = json.load(f)['bounding_box']
assert bbox == {'south': -23.575, 'west': -46.675, 'north': -23.545, 'east': -46.635}, bbox
with open('data/venues.json') as f:
    venues = json.load(f)
assert len(venues) > 0, 'empty catalog'
for v in venues:
    assert -23.575 - 1e-6 <= v['lat'] <= -23.545 + 1e-6, v
    assert -46.675 - 1e-6 <= v['lon'] <= -46.635 + 1e-6, v
print('CATALOG-OK', len(venues), 'venues')
"</automated>
  </verify>
  <done>config/catalog_build.json's bounding_box holds exactly the Sao Paulo coordinates; data/venues.json is non-empty and every venue's lat/lon falls within that new bbox; the pre-existing 32-test unit suite for scripts/build_catalog.py still passes unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Recenter the dashboard's initial map view on the new bbox</name>
  <files>iris/PopHeat/www/dashboard.html</files>
  <action>
    Read iris/PopHeat/www/dashboard.html's DOMContentLoaded handler near the end of the file first. It currently opens the Leaflet map with a hardcoded setView(...) call at a fixed center and zoom, described by the one-line comment directly above it as covering the prior demo city's historic-center bbox from Phase 1 -- that comment and call are now stale, since Task 1 regenerated the catalog with no venues left inside that old area.

    Replace that setView(...) call (and its stale comment) with `map = L.map('map').fitBounds([[-23.575, -46.675], [-23.545, -46.635]], {padding: [20, 20]})` -- the exact bounding_box Task 1 wrote into config/catalog_build.json, expressed as [[south, west], [north, east]] per Leaflet's LatLngBounds convention, with a small padding so edge venues aren't clipped against the viewport edge. Using fitBounds instead of a hand-picked center+zoom keeps the initial view correct for whatever bbox is configured, regardless of its exact width/height (this one is wider than the prior area's), matching specs/venue-sourcing.spec R3's intent that changing the coverage area needs no other code guesswork. Update the comment above the call to name the new area (Sao Paulo -- Paulista/Jardins/Pinheiros/Vila Madalena) in the same ASCII-only style as the comment it replaces (see the file's own MIDDOT note for why ASCII-only matters here). Do not touch anything else in the file -- tile layer setup, marker rendering, popup HTML-escaping, the stale-data banner, and the 10-second refresh loop are unrelated, already-verified Phase 3 behavior.
  </action>
  <verify>
    <automated>cd /home/jean/git/popheat && grep -Fq 'fitBounds([[-23.575, -46.675], [-23.545, -46.635]]' iris/PopHeat/www/dashboard.html && ! grep -Fq '41.1457' iris/PopHeat/www/dashboard.html && ! grep -Fq '-8.6112' iris/PopHeat/www/dashboard.html && echo DASHBOARD-OK</automated>
  </verify>
  <done>dashboard.html's initial map view is built from the new bbox via fitBounds; the old hardcoded center/zoom coordinates are gone from the file; every other section of the file is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Purge pre-swap readings from IRIS, verify the new catalog live, and commit</name>
  <files>config/catalog_build.json, iris/PopHeat/www/dashboard.html</files>
  <precondition>From repo root, `docker compose ps iris` shows the container `Up` (running, not paused/exited), and `curl -sf http://localhost:52773/csp/popheat/api/status` returns `{"running":true}` -- CatalogPollingService must already be polling for this task's purge to be refilled with fresh venues afterward. Never run `docker compose down` at any point in this task (per docker-compose.yml's durability warning) -- only `exec` into the already-running container.</precondition>
  <action>
    PopHeat.Reading has no time-window cutoff in its LatestPerVenue/CountsByHeatLevel queries (iris/PopHeat/Reading.cls) -- every venue_id ever inserted keeps showing its last reading forever, even after Task 1 stops producing new readings for venue_ids no longer in the catalog. Without clearing the rows already written for the prior area's venue_ids, the dashboard would show a stale, frozen mix of both areas instead of the live catalog alone. PopHeat.Reading is insert-only by pipeline design (the class's own comment, "Do not add UPDATE/DELETE logic against this class," refers to the ingestion pipeline's code path) -- this purge is a one-time administrative reset run directly against the database, not a change to any pipeline code.

    From repo root, bulk-delete every row currently in PopHeat.Reading via the container's IRIS session (same ObjectScript-heredoc pattern docker/init-production.sh already uses): run `docker compose exec -T iris iris session IRIS -U POPHEAT` with heredoc input `set sc = ##class(PopHeat.Reading).%DeleteExtent()` then `if 'sc { do $System.Status.DisplayError(sc) }` then `halt`. If %DeleteExtent is ever unavailable, fall back to `set rs = ##class(%SQL.Statement).%ExecDirect(,"DELETE FROM PopHeat.Reading")`. Do not touch PopHeat.BatchTelemetry -- it holds no venue identity or coordinates, so it needs no reset -- and never run this against any namespace other than POPHEAT.

    Wait at least 8 seconds afterward -- CatalogPollingService polls every 3 seconds (settings.py's CallInterval=3), so 8 seconds covers at least two full poll-and-persist cycles -- before checking results, so the just-purged table has had time to refill purely from the new catalog.

    Finally, commit exactly the two source files this plan changed. Confirm the index is empty first (`git diff --cached --quiet`), stage only `git add -- config/catalog_build.json iris/PopHeat/www/dashboard.html`, confirm the staged path set is exactly those two files (`git diff --cached --name-only`), then commit with subject `feat(venue-sourcing): swap demo city bbox to Sao Paulo`, a body paragraph noting the venue-sourcing bounding box now targets Paulista/Jardins/Pinheiros/Vila Madalena in Sao Paulo instead of the prior demo city, that data/venues.json is regenerated locally and gitignored (not part of this commit), and that pre-swap IRIS readings were purged so the live dashboard reflects only the new catalog, ending with the trailer `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` on its own line. Never stage or commit `.planning/config.json` or any other pending change already present in the working tree -- this commit is scoped to exactly those two files.
  </action>
  <verify>
    <automated>cd /home/jean/git/popheat && sleep 8 && python3 -c "
import json, urllib.request
venues = json.load(urllib.request.urlopen('http://localhost:52773/csp/popheat/api/venues', timeout=10))
counts = json.load(urllib.request.urlopen('http://localhost:52773/csp/popheat/api/counts', timeout=10))
assert len(venues) > 0, 'no venues returned after purge+refill'
for v in venues:
    assert -23.575 - 1e-6 <= v['lat'] <= -23.545 + 1e-6, v
    assert -46.675 - 1e-6 <= v['lon'] <= -46.635 + 1e-6, v
assert sum(counts.values()) == len(venues), (counts, len(venues))
print('LIVE-API-OK', len(venues), 'venues')
" && UNCOMMITTED=$(git status --porcelain -- config/catalog_build.json iris/PopHeat/www/dashboard.html) && [ -z "$UNCOMMITTED" ] && SUBJ=$(git log -1 --format=%s) && MSG=$(git log -1 --format=%B) && [[ "$SUBJ" == *'Sao Paulo'* ]] && [[ "$MSG" == *'Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>'* ]] && echo COMMIT-OK</automated>
  </verify>
  <done>Every venue returned by /venues falls inside the new bbox and none inside the prior area; /counts sums to the same total; a new commit on main touches exactly config/catalog_build.json and iris/PopHeat/www/dashboard.html with the required subject and trailer; the working container was never taken down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host/exec to running IRIS container | `docker compose exec` crosses into the container to run a destructive bulk-delete ObjectScript command against live production data |
| public Overpass API to local catalog file | An external, unauthenticated public data source is fetched and written into data/venues.json/venues.raw.json, then read by the running production |
| local working tree to git history (main) | Uncommitted local edits become a permanent, shared commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-q10-01 | Tampering | `docker compose exec ... %DeleteExtent()` against PopHeat.Reading | medium | mitigate | Command is fixed verbatim in the plan (not dynamically constructed), explicitly scoped to the `-U POPHEAT` namespace and the `PopHeat.Reading` class only; PopHeat.BatchTelemetry is explicitly left untouched; the precondition confirms the container is up and the production is running before the purge runs, so the table refills from the live poller within seconds instead of staying empty. |
| T-q10-02 | Tampering / Denial of Service | `docker compose down` accidentally destroying all IRIS data | high | mitigate | Task 3's precondition and action both explicitly forbid `docker compose down`; only `exec`/`ps`/`curl` are used, per docker-compose.yml's own durability warning (WR-02). |
| T-q10-03 | Tampering | Git commit accidentally including the unrelated pending change already sitting in the working tree (`.planning/config.json`) | low | mitigate | Task 3 requires an empty-index guard before staging, stages only the two intended paths by explicit pathspec (never `git add -A`/`git add .`), and verifies the committed path set afterward. |
| T-q10-04 | Denial of Service | Live Overpass fetch in Task 1 fails/times out mid-swap, corrupting or losing the catalog | low | mitigate | `scripts/build_catalog.py`'s `write_json_atomic` + fail-closed `run()` (VENU-06) already guarantee a failed fetch leaves the prior catalog file untouched and exits non-zero; Task 1's action requires checking the exit code before proceeding. |
| T-q10-05 | Information Disclosure | Dashboard/API remain unauthenticated (AutheEnabled=64) after this change | low | accept | Pre-existing, deliberate Phase 3 design (D-06) -- out of scope for this task, which only changes which venues are shown, not who can see them. |
| T-q10-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable -- this task installs no new package; `scripts/build_catalog.py` is stdlib-only (already the case before this task) and the container already has the packages `docker/init-production.sh` previously installed. |
</threat_model>

<verification>
Run each task's own `<automated>` verify from repo root, in task order (Task 1 before Task 2/3 -- Task 2 and Task 3 assume the new catalog exists, and Task 3 assumes Task 2's dashboard edit is already on disk). After all three: `curl -s http://localhost:52773/csp/popheat/api/venues | python3 -c "import json,sys; print(len(json.load(sys.stdin)))"` should print a positive venue count, `curl -s http://localhost:52773/csp/popheat/api/status` should still show `{"running":true}` (the production was never stopped), and `git log --oneline -1` should show the new `feat(venue-sourcing): ...` commit.
</verification>

<success_criteria>
- config/catalog_build.json's bounding_box is exactly the Sao Paulo values, committed on main.
- data/venues.json holds only venues inside that bbox (regenerated locally, correctly gitignored, not committed).
- iris/PopHeat/www/dashboard.html's initial map view is built from the same bbox via fitBounds; the old hardcoded center/zoom is gone.
- Live `/venues` and `/counts` on http://localhost:52773/csp/popheat/api reflect only the new catalog -- no venue from before the swap remains visible.
- The popheat-iris container was never taken down; PopHeat.Production is still running and auto-start remains configured.
- One new commit on main touches exactly the two source files this plan changed, with the required Conventional Commits subject and Co-Authored-By trailer.
</success_criteria>

<output>
Create `.planning/quick/260921-q10-swap-the-demo-city-s-venue-sourcing-boun/260921-q10-SUMMARY.md` when done, recording the new commit SHA, the final Sao Paulo venue count returned by scripts/build_catalog.py, the /counts breakdown observed after the purge+refill, and any deviation from this plan (e.g. if the Overpass container fallback or the %SQL.Statement fallback delete was needed). Do not commit the SUMMARY or this PLAN; the orchestrator handles the docs commit and the STATE.md Quick Tasks Completed entry.
</output>
